[build]
  publish = "public"

[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"

[build.environment]
  NODE_VERSION = "22"

# The API is handled by the Netlify Function at /api/*.
# No permanent Node.js server is required.
